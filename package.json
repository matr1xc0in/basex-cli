{
  "name": "basex-cli",
  "version": "0.0.1",
  "description": "A simple CLI tool to encode/decode baseX input/output",
  "main": "basex-cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "base-x": "^3.0.4",
    "base58check": "^2.0.0"
  }
}
